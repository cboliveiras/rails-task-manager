<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <%= link_to task_path(task.id), method: :delete, data: {confirm: 'Are you sure?'} do %>
    <i class="fas fa-trash" aria-hidden="true"></i>
    <% end %>
  <% end %>
</ul>
<p><%= link_to 'Add another task', new_task_path %></p>